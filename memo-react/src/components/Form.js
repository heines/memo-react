import React from 'react';
import styled from 'styled-components';

const Form = () => {
  return (
    <div>
      <form>
        <textarea
          placeholder="memo"
        ></textarea>
        <button>submit</button>
      </form>
    </div>
  )
};

export default Form;
