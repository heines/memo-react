import styled from 'styled-components';

const Button = styled.button`
  width: 100%;
  padding: 0.5em;
  background: #CCC;
  border: none;
  cursor: pointer;
  outline: none;
`;

export default Button;
